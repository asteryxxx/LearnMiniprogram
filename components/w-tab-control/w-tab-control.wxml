<!--components/w-tab-control/w-tab-control.wxml-->
<view class="tab-control">
  <block wx:for='{{titles}}' wx:key='{{index}}'>
      <view class="tab-item {{currentindex==index?'active':''}}"
      bindtap="itemclick" data-index="{{index}}">
      <text>
          {{item}}
      </text>
      </view>
  </block>
</view>

<view>----修改自定义组件的数据----</view>
<view>{{count}}</view>
